import React from 'react';
import { SectionContainer } from './commentsection.styles';

const CommentSection = ({ comments }) => <SectionContainer></SectionContainer>;

export default CommentSection;
