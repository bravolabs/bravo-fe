import React from 'react';
import { MainContentContainer } from './maincontent.styles';

const MainContent = ({ children }) => <MainContentContainer>{children}</MainContentContainer>;

export default MainContent;
